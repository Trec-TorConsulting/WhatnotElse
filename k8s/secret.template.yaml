apiVersion: v1
kind: Secret
metadata:
  name: whatnot-else-secrets
  namespace: whatnot-else
type: Opaque
stringData:
  # Database root and user passwords
  DB_ROOT_PASSWORD: "CHANGE_ME_IN_CLUSTER"
  DB_PASSWORD: "CHANGE_ME_IN_CLUSTER"
  # Frappe encryption and admin keys
  ADMIN_PASSWORD: "CHANGE_ME_IN_CLUSTER"
  ENCRYPTION_KEY: "CHANGE_ME_IN_CLUSTER"
  # AI APIs
  GEMINI_API_KEY: "CHANGE_ME_IN_CLUSTER"
